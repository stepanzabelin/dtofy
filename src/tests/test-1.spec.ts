// import { getClassSchema } from '../index';
// import { GitStatusOptionsDto } from '../test-env/GitStatusOptionsDto';

// const isJoiError = (result: Joi.ValidationResult<any>) =>
//   result.error instanceof Joi.ValidationError;

describe('Args parser', () => {
  it('parse command', () => {
    // console.log('$$$$$', getClassSchema(GitStatusOptionsDto));
    // expect(
    //   isJoiError(
    //     getClassSchema(GitStatusOptionsDto).validate({
    //       branch: 'master',
    //       'ignore-submodules': 'unknown',
    //     })
    //   )
    // ).toEqual(true);
  });
});
